include .config.mk


# === Build ===
$(bld_DIR)/$(bld_NAME): # sub
	@mkdir -p $(bld_DIR)
	@echo "--- Linking Build ---"
#	Build Command

# sub:
# 	$(MAKE) -C $(sub_DIR) CONFIG_FILE=$(CUR_DIR)/.config.mk ROOT=$(CUR_DIR)

clean:
#	$(MAKE) -C $(sub_DIR) clean

	@echo "--- Cleaning Build ---"
	rm $(bld_DIR)/$(bld_NAME)



.PHONY: clean # sub
