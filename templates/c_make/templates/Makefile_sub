include $(CONFIG_FILE)


# === Units ===
# SRC  =$(call FirstOrderSource,$(src_DIR)/src,EXT) 
# HEAD =$(call FirstOrderSource,$(src_DIR),EXT) 
# OBJ  =$(call SourceToObject,$(SRC),$(obj_DIR))


# === Build ===
subdirectory: $(obj_DIR) # $(OBJ)

.SECONDEXPANSION:
# $(OBJ): $(obj_DIR)/%.o: $(call NameToSource,%,$(SRC),EXT) $(HEAD)
# 	@echo "--- Compiling <$(notdir $<)> in <SUBDIRECTORY> ---"


# --- Directory Management ---
$(obj_DIR):
	@mkdir -p $(obj_DIR)

clean:
	@echo "--- Cleaning <SUBDIRECTORY> ---"
	rm $(obj_DIR)/*.o

.PHONY:  subdirectory clean
